{
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.10.0",
    "fs-extra": "^11.2.0",
    "solc": "^0.8.23-fixed"
  },
  "scripts": {
    "compile": "yarn solcjs --abi --bin --include-path node_modules/ --base-path . -o ./build SimpleStorage.sol"
  }
}
